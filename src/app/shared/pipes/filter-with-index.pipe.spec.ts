import { FilterWithIndexPipe } from './filter-with-index.pipe';

describe('FilterWithIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterWithIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
